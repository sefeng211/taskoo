// Data that are passed from JS are allocated in shared memory
// This function reads the data from the shared memory and
// convert them into an array in Rust.
fn read_data_from_js(_ptr: *mut u8, _len: usize) {
}
